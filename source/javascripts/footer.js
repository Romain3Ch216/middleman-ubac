$(document).ready(function() {

  // var height = $('.jumbotron').last().offset().top + $('#jumbotron-3 .card-text').height() + $('#jumbotron-3 .img-container').height() + 100;
  // var body = $('body').height() - $('.footer').height();
  // if (body > height) {
  //   $('.footer').offset({top: body});
  // } else {
  //   $('.footer').offset({top: height + 50});
  // }
  // $(window).resize(function() {
  //   var height = $('.jumbotron').last().offset().top + $('.jumbotron').last().height();
  //   $('.footer').offset({top: height + 50});
  // });
});
